export function calculateLastYearExpenditure(expenses) {
    //     // Get the current date
    //     const currentDate = new Date();
        
    //     // Get the year of the current date
    //     const currentYear = currentDate.getFullYear();
        
    //     // Initialize total expenditure to keep track of the total amount spent
    //     let totalExpenditure = 0;
        
    //     // Iterate through each expense in the array
    //     for (let expense of expenses) {
    //         // Parse the date string of the expense
    //         const expenseDate = new Date(expense.date);
            
    //         // Get the year of the expense
    //         const expenseYear = expenseDate.getFullYear();
            
    //         // Check if the expense was received in the last year
    //         if (expenseYear === currentYear - 1) {
    //             // Add the expense amount to the totalExpenditure
    //             totalExpenditure += expense.amount < 0 ? expense.amount : 0;
    //         }
    //     }
        
    //     // Return the total amount spent on items that incurred a cost in the last year
    //     return totalExpenditure;
    }